bundle:
  name: ganesh_udemy
  uuid: 8340133d-debb-4463-9d4e-403be8951ee1

include:
  - resources/*.yml

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: dbc-a01bc12e-eb83.cloud.databricks.com
    variables:
      catalog: databricks_ganesh
      schema: ${workspace.current_user.short_name}

    permissions:
      - group_name: admins
        level: CAN_MANAGE

    presets:
      name_prefix: ${workspace.current_user.short_name}
      source_linked_deployment: false

  prod:
    mode: production
    workspace:
      host: dbc-a01bc12e-eb83.cloud.databricks.com
      root_path: /Workspace/Users/ganeshrupanar10@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricks_ganesh
      schema: prod
    permissions:
      - user_name: ganeshrupanar10@gmail.com
        level: CAN_MANAGE
